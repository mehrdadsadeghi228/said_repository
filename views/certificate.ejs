<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Certificate</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .hologram {
            width: 150px;
            height: 150px;
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), rgba(0, 255, 255, 0.6), rgba(255, 0, 255, 0.6), rgba(255, 255, 255, 0.1));
            border-radius: 50%;
            position: absolute;
            bottom: 20px;
            right: 30px;
            background-size: 200% 200%;
            animation: hologram-glow 4s infinite;
        }

        @keyframes hologram-glow {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
    </style>
</head>
<body class="bg-gray-100">
    <div class="container mx-auto py-12">
        <div class="bg-white border-4 border-indigo-500 shadow-lg rounded-lg p-12 relative">
            <h1 class="text-5xl font-bold text-center text-gray-800">Certificate of Completion</h1>
            <p class="text-center text-xl mt-6">This is to certify that</p>
            <h2 class="text-3xl font-semibold text-center text-blue-500 mt-2"><%= cer.name %></h2>
            <p class="text-center text-xl mt-6">has successfully completed</p>
            <h3 class="text-2xl font-semibold text-center text-purple-600 mt-2"><%= cer.NationnalCode %></h3>
            <p class="text-center text-xl mt-6">with Nationnal Code </p>
            <h3 class="text-2xl font-semibold text-center text-purple-600 mt-2"><%= cer.couresName %></h3>
            <p class="text-center text-lg mt-6">IssueDate: <%= cer.issueDate %></p>

            <div class="border-t-2 border-gray-300 mt-10"></div>
            
            <!-- Signature and Hologram -->
            <div class="flex justify-between items-center mt-6">
                <p class="text-xl font-semibold text-left">ExpiryDate: <%= cer.expiryDate %></p>
                <div class="text-right">
                    <p class="text-xl font-semibold">Signed by</p>
                    <img src="/img/signature.png" alt="Signature" class="w-32">
                </div>
            </div>
            <!-- Hologram -->
            <div class="hologram"></div>
        </div>
    </div>
</body>
</html>
